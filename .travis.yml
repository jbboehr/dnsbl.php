language: php

php:
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - 7.3
  - master

matrix:
  fast_finish: true
  allow_failures:
    - php: 'master'

before_script:
  - composer install

branches:
  only:
    - master
    - travis
    - ci

script:
  - phpunit --coverage-text

